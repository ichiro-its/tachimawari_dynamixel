name: Build and Test
on:
  pull_request:
    branches: [master]
  push:
    branches: [master]
jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checking out
        uses: actions/checkout@v2.3.4
        with:
          path: tachimawari_dynamixel

      - name: Build and test workspace
        uses: ichiro-its/ros2-build-and-test-action@main
